// Gerar a soma de dois números inteiros
void main (){
    int n1 = Integer.parseInt(IO.readln("Insira um número: "));
    int n2 = Integer.parseInt(IO.readln("Insira um número: "));

    int soma = n1 + n2;

    IO.println("A soma dos dois números é:" + soma);

}